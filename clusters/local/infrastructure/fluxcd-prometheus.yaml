apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: prometheus-helmrelease
  namespace: prometheus
  annotations:
    config-updated: "${CONFIG_TIMESTAMP}"
spec:
  interval: 60m # Shorter interval for local development
  valuesFrom:
    - kind: ConfigMap
      name: prometheus-base-values
      valuesKey: base.values.yaml
    - kind: ConfigMap
      name: prometheus-level-values
      valuesKey: lcl.values.yaml
  values:
    replicaCount: 1 # Single replica for local
