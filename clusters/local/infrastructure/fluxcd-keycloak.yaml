apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: keycloak-helmrelease
  namespace: keycloak
spec:
  interval: 1m # Shorter interval for local development
  valuesFrom:
    - kind: ConfigMap
      name: keycloak-base-values
      valuesKey: base.values.yaml
      optional: false
    - kind: ConfigMap
      name: keycloak-level-values
      valuesKey: lcl.values.yaml
      optional: false
