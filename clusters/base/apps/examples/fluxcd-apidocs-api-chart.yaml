apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: apidocs-api-chart-img-repo
  namespace: flux-system
spec:
  interval: 240m
  image: ${IMAGE_REGISTRY:=ghcr.io/bouc-io}/application/apidocs/apidocs-api-server
  provider: generic
  secretRef:
    name: ${REGISTRY_SECRET:=registry-credentials}
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: apidocs-api-chart-imgpolicy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: apidocs-api-chart-img-repo
  policy:
    semver:
      range: "${IMAGE_TAG_RANGE:=x.x.x}" # Override IMAGE_TAG_RANGE in cluster-vars to pin (e.g. 1.0.x)
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: apidocs-api-chart-helmrelease
  namespace: default
spec:
  interval: 240m
  chart:
    spec:
      chart: apidocs-api-chart
      version: "0.x.x"
      sourceRef:
        kind: HelmRepository
        name: boucio-charts
        namespace: default
      interval: 240m
  valuesFrom:
    - kind: ConfigMap
      name: apidocs-api-chart-values
      valuesKey: values.yaml
  # Drives the object names the chart renders. The Istio route and the oauth2-proxy
  # upstreams both target apidocs-api-chart.default.svc.cluster.local, which only
  # resolves because the release is named apidocs-api-chart (not the HelmRelease name).
  releaseName: apidocs-api-chart
  install:
    crds: Create
    disableWait: true
  upgrade:
    crds: CreateReplace
    disableWait: true
  values:
    image:
      registry: ${IMAGE_REGISTRY:=ghcr.io/bouc-io}
      imagePullPolicy: IfNotPresent
      # Dynamic tag to be updated by Image Automation
      tag: "latest" # {"$imagepolicy": "flux-system:apidocs-api-chart-imgpolicy:tag"}
    replicaCount: 1
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageUpdateAutomation
metadata:
  name: apidocs-api-chart-auto-imgupdate
  namespace: flux-system
spec:
  interval: 240m
  sourceRef:
    kind: GitRepository
    name: flux-system
  git:
    checkout:
      ref:
        branch: master
    commit:
      author:
        email: martin.cote@gmail.com
        name: "Martin Cote"
      messageTemplate: "{{range .Changed.Changes}}{{print .OldValue}} -> {{println .NewValue}}{{end}}"
    push:
      branch: master
  update:
    path: ./clusters/base/apps/examples
    strategy: Setters
